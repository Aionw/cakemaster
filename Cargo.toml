[package]
name = "cakemaster"
version = "0.1.0"
edition = "2024"
description = "Tokio implementation of the yalantinglibs coro_rpc v0 wire protocol"
license = "Apache-2.0"

[dependencies]
md-5 = "0.11"
tokio = { version = "1", features = ["io-util", "macros", "net", "rt-multi-thread", "signal", "sync", "time"] }
